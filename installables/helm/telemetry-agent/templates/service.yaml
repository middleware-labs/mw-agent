apiVersion: v1
kind: Service
metadata:
  name: {{ include "telemetry-agent.fullname" . }}
  labels:
    {{- include "telemetry-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "telemetry-agent.selectorLabels" . | nindent 4 }}
