"use strict";
/*
 * Copyright The OpenTelemetry Authors
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      https://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.BoundObservable = exports.BoundHistogram = exports.BoundUpDownCounter = exports.BoundCounter = exports.BaseBoundInstrument = void 0;
const api_1 = require("@opentelemetry/api");
const api = require("@opentelemetry/api-metrics");
/**
 * This class represent the base to BoundInstrument, which is responsible for generating
 * the TimeSeries.
 */
class BaseBoundInstrument {
    constructor(attributes, _disabled, _valueType, _aggregator) {
        this._disabled = _disabled;
        this._valueType = _valueType;
        this._aggregator = _aggregator;
        this._attributes = attributes;
    }
    update(value) {
        if (this._disabled)
            return;
        if (typeof value !== 'number') {
            api_1.diag.error(`Metric cannot accept a non-number value for ${Object.values(this._attributes)}.`);
            return;
        }
        if (this._valueType === api.ValueType.INT && !Number.isInteger(value)) {
            api_1.diag.warn(`INT value type cannot accept a floating-point value for ${Object.values(this._attributes)}, ignoring the fractional digits.`);
            value = Math.trunc(value);
        }
        this._aggregator.update(value);
    }
    getAttributes() {
        return this._attributes;
    }
    getAggregator() {
        return this._aggregator;
    }
}
exports.BaseBoundInstrument = BaseBoundInstrument;
/**
 * BoundCounter allows the SDK to observe/record a single metric event. The
 * value of single instrument in the `Counter` associated with specified Attributes.
 */
class BoundCounter extends BaseBoundInstrument {
    constructor(attributes, disabled, valueType, aggregator) {
        super(attributes, disabled, valueType, aggregator);
    }
    add(value) {
        if (value < 0) {
            api_1.diag.error(`Counter cannot descend for ${Object.values(this._attributes)}`);
            return;
        }
        this.update(value);
    }
}
exports.BoundCounter = BoundCounter;
/**
 * BoundUpDownCounter allows the SDK to observe/record a single metric event.
 * The value of single instrument in the `UpDownCounter` associated with
 * specified Attributes.
 */
class BoundUpDownCounter extends BaseBoundInstrument {
    constructor(attributes, disabled, valueType, aggregator) {
        super(attributes, disabled, valueType, aggregator);
    }
    add(value) {
        this.update(value);
    }
}
exports.BoundUpDownCounter = BoundUpDownCounter;
/**
 * BoundMeasure is an implementation of the {@link BoundMeasure} interface.
 */
class BoundHistogram extends BaseBoundInstrument {
    constructor(attributes, disabled, valueType, aggregator) {
        super(attributes, disabled, valueType, aggregator);
    }
    record(value) {
        this.update(value);
    }
}
exports.BoundHistogram = BoundHistogram;
/**
 * BoundObservable is an implementation of the {@link BoundObservable} interface.
 */
class BoundObservable extends BaseBoundInstrument {
    constructor(attributes, disabled, valueType, aggregator) {
        super(attributes, disabled, valueType, aggregator);
    }
}
exports.BoundObservable = BoundObservable;
//# sourceMappingURL=BoundInstrument.js.map